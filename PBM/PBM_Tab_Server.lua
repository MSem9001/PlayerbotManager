PBM = PBM or {}

-- ── Server tab panel ───────────────────────────────────────────
-- Called from PBM_BottomTabs.lua:BuildBottomTabs as:
--   PBM.BuildServerPanel(panel, ctx)
-- Placeholder — add server controls here.

function PBM.BuildServerPanel(pf, ctx)
    -- placeholder
end
